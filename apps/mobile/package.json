{
  "name": "@acuba/mobile",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "dev": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "build": "expo export --platform ios --platform android --output-dir dist",
    "lint": "eslint App.tsx index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@acuba/contracts": "workspace:*",
    "expo": "~53.0.9",
    "expo-status-bar": "~2.2.3",
    "react": "19.0.0",
    "react-native": "0.79.2"
  },
  "devDependencies": {
    "@acuba/config": "workspace:*",
    "@types/react": "~19.0.10",
    "eslint": "^9.27.0",
    "typescript": "~5.8.3"
  }
}
